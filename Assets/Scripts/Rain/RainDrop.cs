using System;
using UnityEngine;
using Random = UnityEngine.Random;

public class RainDrop : MonoBehaviour {
    
}


/*
 *
    [SerializeField] private float dropMass = 200f;

    public void Init(float rainDropLifetime) {
        Destroy(gameObject, rainDropLifetime);
    }
    private void Awake() {
        float xPosition;
        float yPosition;
        float zPosition;
        
        Vector3[] positions = new Vector3[3];

        for (int j = 0; j < 3; j++) 
            for (int i = 0; i < 3; i++) 
                positions[j][i] = Random.Range(-1, 1);
        
    }
 * 
 */